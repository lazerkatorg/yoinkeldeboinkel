name: Windows RDP with Pinggy

on: [push, workflow_dispatch]

jobs:
  build:
    runs-on: windows-latest
    timeout-minutes: 9999

    steps:
    - name: Enable Remote Desktop
      run: |
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' -name "fDenyTSConnections" -Value 0
        Enable-NetFirewallRule -DisplayGroup "Remote Desktop"
        Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' -name "UserAuthentication" -Value 0
    
    - name: Set Password
      run: Set-LocalUser -Name "runneradmin" -Password (ConvertTo-SecureString -AsPlainText "Password123!" -Force)
    
    - name: Install OpenSSH Client
      run: |
        Add-WindowsCapability -Online -Name OpenSSH.Client~~~~0.0.1.0
    
    - name: Start RDP Tunnel
      shell: pwsh
      run: |
        # Give OpenSSH a moment to install
        Start-Sleep -Seconds 5
        
        Write-Host "Starting tunnel - the address will appear below when connected..."
        Write-Host ""
        
        # Run SSH and capture ALL output
        & ssh -o StrictHostKeyChecking=no -p 443 -R0:127.0.0.1:3389 qr+tcp@free.pinggy.io 2>&1 | ForEach-Object {
          Write-Host $_
          if ($_ -match "tcp://") {
            Write-Host ""
            Write-Host "========================================="
            Write-Host "RDP CONNECTION DETAILS:"
            Write-Host $_
            Write-Host "Username: runneradmin"
            Write-Host "Password: Password123!"
            Write-Host "========================================="
            Write-Host ""
          }
        }
